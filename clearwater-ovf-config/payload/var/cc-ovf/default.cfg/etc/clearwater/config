# This IF is handy for testing these files when they're in other locations
if [ "${BASH_ARGV:=unset}" != "unset" ]; then
   mydir=$(cd $(dirname ${BASH_ARGV[0]});pwd)
else
   mydir=/etc/clearwater
fi
. ${mydir}/local_config
. ${mydir}/shared_config
